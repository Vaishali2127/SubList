import React from 'react'
import { Wrapper, Card, Para, Button } from './styles'
const Navbar = () => {
    return (
        <Wrapper>
            <Card>
                <Para>Welcome!</Para>
                <Button>Login</Button>
            </Card>
        </Wrapper>
    )
}

export default Navbar
